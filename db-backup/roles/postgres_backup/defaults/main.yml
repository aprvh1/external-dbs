---
# PostgreSQL Backup to S3 (via pg_basebackup physical snapshot)

postgres_backup_s3_bucket: ""
postgres_backup_s3_prefix: "postgres-backups"
postgres_backup_local_tmp_dir: "/tmp"

# These are inherited from roles/postgres/defaults/main.yml:
# - postgres_bin_dir
# - postgres_port
# - postgres_replication_user
# - postgres_replication_password
